const MacbookFortalIcon = ({ size }: { size: number }) => {
  return (
    <img
      className="rounded-md"
      src="macbook-fortaleza.png"
      alt="Macbook Fortaleza"
      style={{ height: `${size}px` }}
    />
  )
}

export default MacbookFortalIcon
